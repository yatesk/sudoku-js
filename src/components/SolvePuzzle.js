function SolvePuzzle(gridID, value, updateGrid, revealedCell, invalidCellNumber, isGamePaused) {

    return (
        <input type="button" id="solvePuzzleButton" value="Solve Puzzle" onClick={solvePuzzle}/>
    );
}

export default SolvePuzzle;